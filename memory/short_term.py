"""Short-term memory = workflow state persisted in SQLite (backend/core/database.py) + in-memory logs"""
# Intentionally minimal for M0-M4. Long-term memory can be added as ./memory/long_term.json later
